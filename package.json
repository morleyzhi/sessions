{
  "name": "sessions",
  "version": "1.0.0",
  "description": "Local browser for Claude Code, Codex, and Cursor session history",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . Sessions --platform=darwin --arch=arm64 --app-bundle-id=com.morleyzhi.sessions --out=dist --overwrite",
    "install-app": "bash scripts/install-app.sh"
  },
  "license": "MIT",
  "devDependencies": {
    "@electron/packager": "^18.4.4",
    "electron": "^44.0.0"
  }
}
